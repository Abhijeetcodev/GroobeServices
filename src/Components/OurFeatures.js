import React from 'react'

import { Hands } from '../assets';

const OurFeatures = () => {
    return (
        <div  style={{backgroundImage:`url(${Hands})` }} >
       
        </div>
    );
}

export default OurFeatures;